<ArgonTest>

    <Name>Class with different parameters</Name>

    <InputSource name="index.argon"><![CDATA[

        module _

        import Argon.Core::*

        class MyClass(T: type) <: Object
        end

        def myFunc(a: MyClass Int): MyClass String =
            a

        def main(): () do
        end

    ]]></InputSource>

    <ExpectedError>TypeError</ExpectedError>

</ArgonTest>
