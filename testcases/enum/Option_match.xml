<ArgonTest>

    <Name>Option (match)</Name>

    <InputSource name="index.argon">
        module _

        import Argon.Core::*

        enum Option [T: type]
            Some(value: T)
            None
        end

        proc main(): () do
            let o = Option.Some "ABC"
            match o
                case Option.Some (val s) =>
                    puts s
                case Option.None =>
                    puts "(empty)"
            end
        end

    </InputSource>

    <ExpectedOutput>ABC</ExpectedOutput>

</ArgonTest>
