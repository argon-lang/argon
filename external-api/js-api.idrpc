module dev.argon.build.testrunner.node.ExternalApi

def executeJS(entrypoint: String, files: Array<FileInfo>): ExecutionResult;

record FileInfo {
	name: String;
	content: String;
}

enum ExecutionResult {
	Success(output: String);
	Failure(error: String);
}
